from fastapi import FastAPI, File, UploadFile, Form, HTTPException
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse
import openai
import os
import json
from docx import Document
from docx.shared import Inches
from datetime import datetime

app = FastAPI()

app.mount("/static", StaticFiles(directory="static"), name="static")

UPLOAD_DIR = "uploads"
REPORTS_DIR = "reports"
os.makedirs(UPLOAD_DIR, exist_ok=True)
os.makedirs(REPORTS_DIR, exist_ok=True)

#openai.api_key = ""

json_data = {}


@app.post("/upload/")
async def upload_json(file: UploadFile = File(...)):
    """Загружает JSON-файл и парсит данные"""
    global json_data
    file_path = os.path.join(UPLOAD_DIR, file.filename)

    try:
        if not file.filename.endswith(".json"):
            raise HTTPException(status_code=400, detail="Файл должен быть в формате JSON.")

        contents = await file.read()
        json_data = json.loads(contents.decode("utf-8"))

        with open(file_path, "w", encoding="utf-8") as f:
            json.dump(json_data, f, ensure_ascii=False, indent=4)

        return {"filename": file.filename, "message": "JSON успешно загружен"}

    except json.JSONDecodeError:
        raise HTTPException(status_code=400, detail="Ошибка парсинга JSON. Проверьте содержимое файла.")
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Ошибка сервера: {str(e)}")


@app.post("/generate_report/")
async def generate_report(city: str = Form(...)):
    """Генерирует отчет о рынке недвижимости на основе JSON-данных"""
    if not json_data:
        raise HTTPException(status_code=400, detail="JSON-файл не загружен. Сначала загрузите данные.")

    doc = Document()
    doc.add_heading(f'Отчет о рынке недвижимости в {city}', level=1)

    current_date = datetime.today().strftime('%d.%m.%Y')

    prompts = [
        f"""Составь подробный отчет по анализу общих сведений об объекте оценки. Для этого проанализируй данные: {json_data.get('Общие характеристики', 'Нет данных')}. Опиши их связным текстом. Для составления отчета используй следующую структуру:
        1. Общие сведения об объекте оценки""",

        f"""Составь подробный отчет по анализу влияния общей политической и социально-экономической обстановки в России на рынок недвижимости. Укажи достоверные цифры на {current_date} и приведи конкретные примеры. Для анализа использовать публикации только надежных источников. Укажи ссылки на используемые публикации. Для составления отчета используй следующую структуру: 
        2. Анализ влияния общей политической и социально-экономической обстановки в России на рынок недвижимости
            2.1. Социально-экономическая обстановка в РФ в 2024 году,
            2.2. Основные экономические факторы,
            2.3. Основные экономические показатели,
            2.4. Продажа недвижимости,
            2.5. Аренда недвижимости,
            2.6. Ставки доходности,
            2.7. Окупаемость инвестиций,
            2.8. Тенденции и перспективы,
            2.9. Прогноз на 2025 год,
            Заключение""",

        f"""Составь подробный отчет по анализу влияния общей политической и социально-экономической обстановки в городе {city} на рынок недвижимости. Укажи достоверные цифры на {current_date} и приведи конкретные примеры. Для анализа использовать публикации только надежных источников. Укажи ссылки на используемые публикации. Для составления отчета используй следующую структуру: 
        3. Анализ влияния общей политической и социально-экономической обстановки на рынок недвижимости
            3.1. Продажа недвижимости,
            3.2. Динамика спроса и предложения,
            3.3. Аренда недвижимости,
            3.4. Тенденции и перспективы,
            Заключение""",

        f"""Составь подробный отчет по анализу дома, в котором находится оцениваемая квартира. Для этого проанализируй данные: {json_data.get('Характеристики здания', 'Нет данных')}. Опиши их связным текстом. Для составления отчета используй следующую структуру:
        4. Информация о доме, в котором находится оцениваемая квартира""",

        f"""Составь подробный отчет по анализу местоположения объекта оценки и ближайшего окружения. Для составления отчета используй следующую структуру:
        5. Анализ местоположения объекта оценки. Ближайшее окружение
            5.1. Территориально-функциональная зона (В данном подпункте проанализируй территориально-функциональную зону: {json_data.get('Зона', 'Нет данных')}, в которой находится объект оценки),
            5.2. Ближайшее окружение (В данном подпункте проанализируй данные: {json_data.get('Расстояние до ближайщего объекта, м"', 'Нет данных')} и {json_data.get('Количестов объектов в радиусе 900м', 'Нет данных')}. Опиши их связным текстом),
            5.3. Зона местоположения (В данном подпункте проанализируй данные: {json_data.get('Рейтинг зоны местонахождения', 'Нет данных')}. Опиши их связным текстом),
            Заключение""",

        f"""Составь подробный отчет по анализу фактических данных о ценах и арендных ставках в городе {city}. Для этого проанализируй данные : {json_data.get('Удельная арендная ставква, руб./кв.м. (гистограмма)', 'Нет данных')}, {json_data.get('Удельная цена, тыс. руб./кв.м. (гистограмма)', 'Нет данных')}. Опиши их связным текстом. Для составления отчета используй следующую структуру:
        6. Анализ фактических данных о ценах и арендных ставках""",

        f"""Составь подробный отчет по анализу основных факторов, влияющих на цены недвижимости в городе {city}. Укажи достоверные цифры на {current_date} и приведи конкретные примеры. Для анализа использовать публикации только надежных источников. Укажи ссылки на используемые публикации. Для составления отчета используй следующую структуру: 
        7. Анализ основных факторов, влияющих на цены недвижимости
            7.1. Перечень основных факторов,
            7.2. Характер влияния факторов,
            Заключение""",

        f"""Составь подробный отчет по анализу предложений на продажу квартир в городе {city}. Для этого проанализируй данные: {json_data.get('Общая площадь кв.м. (гистограмма)', 'Нет данных')}, {json_data.get('Состояние отделки (круговая диаграмма)', 'Нет данных')}, {json_data.get('Количество комнат (круговая диаграмма)', 'Нет данных')}. Опиши их связным текстом. Для составления отчета используй следующую структуру:
        8. Анализ предложений на продажу квартир""",

        f"""Составь подробный отчет по анализу ликвидности квартир в городе {city}. Для составления отчета используй следующую структуру:
        9. Анализ ликвидности квартир""",

        f"""Составь подробный отчет по анализу спроса на квартиры в городе {city}. Для составления отчета используй следующую структуру:
        10. Анализ спроса на квартиры""",

        f"""Составь подробный отчет по основным выводам относительно рынка недвижимости в городе {city}. Для этого проанализируй данные: {json_data.get('INF-Оценка', 'Нет данных')}. Опиши их связным текстом. Укажи достоверные цифры на {current_date} и приведи конкретные примеры. Для анализа использовать публикации только надежных источников. Укажи ссылки на используемые публикации. Для составления отчета используй следующую структуру:
        11. Основные выводы относительно рынка недвижимости
            11.1. Динамика рынка,
            11.2. Спрос и предложение,
            11.3. Объем продаж и емкость рынка,
            11.4. Мотивация покупателей и продавцов,
            11.5. Ликвидность,
            11.6. Колебания цен,
            11.7. Другие выводы"""
    ]

    for i, prompt in enumerate(prompts, 1):
        try:
            response = openai.ChatCompletion.create(
                model="gpt-4o-mini",
                messages=[{"role": "system", "content": prompt}]
            )
            text = response['choices'][0]['message']['content']
        except openai.error.OpenAIError as e:
            text = f"Ошибка при генерации текста: {str(e)}"

        doc.add_heading(f'Раздел {i}', level=2)

        section_data = json_data.get(list(json_data.keys())[i-1], {})
        image_path = section_data.get("Изображение") if isinstance(section_data, dict) else None

        if image_path and os.path.exists(image_path):
            doc.add_picture(image_path, width=Inches(5.5))
            doc.add_paragraph(f"Рис. {i}: {os.path.basename(image_path)}")

        doc.add_paragraph(text)

    report_path = os.path.join(REPORTS_DIR, f"report_{city}.docx")
    doc.save(report_path)

    return {"report_path": report_path}


@app.get("/download_report/")
async def download_report(city: str):
    """Отправляет пользователю готовый отчет"""
    report_path = os.path.join(REPORTS_DIR, f"report_{city}.docx")
    if os.path.exists(report_path):
        return FileResponse(report_path, filename=f"report_{city}.docx")
    raise HTTPException(status_code=404, detail="Отчет не найден")


@app.get("/")
async def serve_home():
    return FileResponse("static/index.html")